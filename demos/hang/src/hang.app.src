{application, hang,
  [{description, "SIP UA that hangs after few seconds of established session"},
   {vsn, "0.1.0"},
   {applications, [kernel, stdlib, gproc, sip]},
   {mod, {hang_app,[]}},
   {start_phases, []},
   {env, []}]}.
